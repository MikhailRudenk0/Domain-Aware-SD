# План воспроизведения baseline Acceptance Rate (first 1 token)

> Версия 2 (2026-08-18). Предыдущая версия плана писалась на VPS без GPU и
> исходила из того, что нужно перебирать dtype/topk_K и искать потерянный
> синтетический датасет. Обе посылки сняты — см. «Что изменилось».

## Цель

Воспроизвести **все 66 per-cluster чисел** для **baseline** (нефайнтюненного
драфтера) со старого графика `results/ar_per_cluster_first1_token.jpg`:

```
Baseline mean = 0.373, std = 0.181   (первый токен ответа)
```

Эталонные 66 чисел извлечены из картинки и лежат в
`results/reference_ar_first1_token.txt` (+ `.json` для скриптов).
Метод извлечения — пиксельный анализ высот столбцов, калибровка по геометрии
оси Y (0.0 → y=141 px, 1.0 → y=26 px). Валидация: извлечённая статистика
`mean=0.368, std=0.187` против подписанной на графике `mean=0.373, std=0.181`
(и для SFT-серии: `0.802/0.147` против `0.809/0.151`) → ошибка извлечения
≈ ±0.02 на столбец. Три последних столбца (`e2e_nlg`, `anli_r1`, `wic`)
частично перекрыты боксом легенды → низкая достоверность, из скоринга
исключаются.

## Что изменилось относительно версии 1

| Было в v1 | Стало |
|---|---|
| «нужна машина с GPU» | Работаем на машине с 2× RTX 3090 (24 ГБ), 62 ГБ RAM |
| Воспроизводим и SFT, и baseline | **SFT-драфтер потерян безвозвратно** → только baseline |
| Нужно восстановить старую синтетику | **Синтетика не нужна**: для first-token AR датасет — это `flan/validation` (промпт + референсный ответ), он идентичен старому |
| Перебор dtype / topk_K как приоритет №1–2 | Главные степени свободы — **параметры сэмплирования обеих моделей** и **индекс «первого токена ответа»** |
| Драфтер под вопросом (Lite-Mistral vs tiny-mixtral) | Фиксировано: `Lite-Mistral-150M-v2-Instruct` |

## КРИТИЧЕСКАЯ НАХОДКА (2026-08-18): таргет считал мусор

`modeling_bamboo.py` написан под transformers 4.41. Его rotary-модуль
регистрирует `inv_freq` / `cos_cached` / `sin_cached` как **non-persistent**
буферы — они вычисляются в `__init__` и не лежат в чекпойнте. Transformers ≥ 5
грузит remote-code модели через meta-device и такие буферы **не
материализует**, поэтому после `from_pretrained` в них неинициализированная
память:

```
inv_freq[:5] = [-1.8e+32, 3.3e-41, nan, 4.2e-41, 1.4e-04]
cos_cached   = все нули
sin_cached   = все нули
```

То есть RoPE был полностью сломан. Симптомы:

| | до фикса | после фикса |
|---|---|---|
| next-token top-1 accuracy на простом английском тексте | 0.079 | **0.711** |
| perplexity там же | 776.9 | **3.1** |
| `"The capital of France is"` → | `'of'` (p=0.057) | **`'Paris'` (p=0.907)** |
| attn_implementation=`sdpa` | NaN | работает |

Проверка, что дело не в весах: те же веса, загруженные в штатный
`MistralForCausalLM`, давали ppl 11.7 и осмысленные предсказания — значит
чекпойнт цел, ломался только Bamboo-путь.

**Следствия:**
- Все прогоны eval с `target_source=model` (включая июльские 66 JSON-ов в
  `eval_results/`) считались против сломанного таргета — их числа
  недействительны. Это и есть главная причина расхождения с исходным графиком,
  а вовсе не dtype/topk_K/ренормализация.
- Фикс: `src/repro/bamboo_fix.py` → `fix_rotary(model)` пересчитывает `inv_freq`
  и cos/sin-кэши после загрузки. Проверка: `python -m src.repro.bamboo_fix`.
- **TODO:** подключить `fix_rotary` в `src/eval/model_loader.py` и перепрогнать
  основной eval-пайплайн.

## Модели (фиксированы)

- Target: `TurboSparse-Mistral-Instruct` (7B, `BambooForCausalLM`, `trust_remote_code=True`), vocab 32064.
- Draft: `Lite-Mistral-150M-v2-Instruct` (150M, `MistralForCausalLM`), vocab 32768.
- **Токенайзеры совпадают**: проверено, id 0..31999 дают идентичные строки в обеих моделях
  (обе — `LlamaTokenizer` на одном SentencePiece). Расхождение только в паддинге
  эмбеддинг-матрицы и в спец-токенах ChatML у таргета (`<|im_end|>`=32000,
  `<|im_start|>`=32001). Сравнение распределений идёт по общей части 0..31999.

## Метрика

```
AR(x) = Σ_v min( p_draft(v | x), p_target(v | x) )      v = 0..31999
```

То есть площадь пересечения двух распределений на первом токене ответа —
**без** ренормализации на top-10 таргета (именно ренормализация в текущем
`src/eval/` схлопывала межкластерный разброс: std 0.03–0.12 вместо 0.18).

Оба распределения берутся **после** применения sampling-параметров
(temperature → top_k → top_p → renormalize), потому что именно преобразованное
распределение участвует в приёме токена при speculative decoding.

## Степени свободы (что перебираем)

### 1. Параметры сэмплирования — главный рычаг
- `T_target`, `T_draft` ∈ {0.6, 0.7, 0.8, 0.9, 1.0, 1.1, 1.2, 1.5} — независимо;
  T задаёт средний уровень AR (T→0 обе в one-hot, AR→top1_match; T→∞ обе к
  равномерному, AR→1).
- `top_p` ∈ {1.0, 0.95, 0.9} (nucleus, применяется к каждой модели),
- `top_k` ∈ {0 (выкл), 50}.

### 2. Что считать «первым токеном ответа» — вторая ось
Из-за спец-токенов и шаблона чата позиция неоднозначна. Варианты промпта:

| Вариант | Вход |
|---|---|
| `plain` | `[BOS] + enc(inputs)` |
| `plain_nl` | `[BOS] + enc(inputs + "\n")` |
| `chatml` | `<\|im_start\|>user\n{inputs}<\|im_end\|>\n<\|im_start\|>assistant\n` (шаблон таргета, без BOS) |
| `chatml_bos` | то же + BOS в начале |

Варианты позиции: `offset k` = логиты, предсказывающие k-й токен ответа
(k = 0, 1, 2; при k>0 в контекст подаются первые k референсных токенов ответа).

### 3. Вторичное (только если п.1–2 не сойдутся)
- dtype таргета (bfloat16 / float32),
- считать softmax по полному словарю модели или только по общим 0..31999,
- ChatML-промпт для таргета и «родной» шаблон для драфтера.

## Метод: кэш логитов + дешёвый свип

Ключ к скорости: логиты **не зависят** от sampling-параметров. Поэтому

1. **Один раз** прогоняем обе модели по `flan/validation` (66 кластеров) для
   каждого варианта промпта и сохраняем логиты в нужных позициях
   (`data/repro_cache/<variant>/<cluster>.npz`, fp16).
2. Свип по (T_target, T_draft, top_p, top_k, offset) считается уже **на кэше** —
   сотни комбинаций за минуты, GPU нужен только для softmax.

Оценка стоимости шага 1: ~3300 сэмплов (50/кластер) × ~500 токенов ≈ 13 мин на
вариант промпта для таргета на одной 3090; два варианта параллельно на двух GPU.

## Скоринг

Для каждой комбинации считаем 66 чисел и сравниваем с эталоном:

- `MAE` и `RMSE` по 63 достоверным кластерам — **основной критерий**,
- `Pearson r` и `Spearman ρ` — воспроизводится ли *форма* профиля (порядок кластеров),
- `|mean − 0.373|`, `|std − 0.181|` — совпадение агрегатов.

Порядок оптимизации: сначала максимизируем корреляцию (правильная форма —
значит правильные промпт/позиция/метрика), потом подгоняем уровень
температурами.

## Этапы

- [x] Извлечь 66 эталонных чисел из `ar_per_cluster_first1_token.jpg`
- [x] Проверить совместимость токенайзеров (id 0..31999 идентичны)
- [x] **Найти и починить сломанный RoPE таргета** (`src/repro/bamboo_fix.py`)
- [x] Шаг 1: кэш логитов для 5 вариантов промпта × offsets 0..7 (`src/repro/cache_first_token.py`)
- [x] Шаг 2: свип sampling-параметров по кэшу (`src/repro/sweep_first_token.py`, `optimize_first_token.py`)
- [x] Шаг 3: полный прогон на всех сэмплах validation (200/кластер)
- [x] Шаг 4: график `results/ar_per_cluster_first1_token_REPRO.png` (+ `_bestcorr`)
- [x] Шаг 5: итоговые числа в `results/repro_ar_first1_token.{json,txt}`

## Итог воспроизведения (2026-08-18)

Лучшая найденная конфигурация (по MAE на 63 достоверных кластерах):

| Параметр | Значение |
|---|---|
| Промпт | `plain_nl`: `[BOS] + enc(inputs + "\n")`, токенайзер таргета, без chat template |
| Позиция | среднее AR по первым токенам ответа (окно 0..7; окно 0..2 почти так же) |
| Метрика | `overlap_full` = Σ min(p_draft, p_target) по общим id 0..31999, без top-K ренормализации |
| T_target / T_draft | 2.0 / 0.7 (альтернатива 1.5 / 0.5 даёт лучший r=0.713) |
| top_p / top_k | 1.0 / выкл |

Качество: **MAE=0.106, RMSE=0.148, Pearson r=0.654, Spearman ρ=0.690;
mean=0.385/std=0.178 против эталонных 0.373/0.181** (агрегаты совпали).
Переводы (8 wmt/para_crawl кластеров) воспроизводятся практически точно.

Систематический остаток: NLI/классификация с опциями (cb, mnli, rte, trec,
definite_pronoun_resolution, story_cloze) выходит **выше** эталона, а
открытая генерация (e2e_nlg, dart, drop, trivia_qa) — **ниже**. Это не
объясняется ни температурами, ни top_p/top_k, ни выбором позиции — вероятно,
старый пайплайн иначе строил промпт для этих задач (например, добавлял
шаблон с опциями ответа) либо считал AR на сгенерированном таргетом
продолжении, а не на референсном ответе.

Проверенные и отклонённые ветки:
- ChatML-промпт: распределения почти дизъюнктны в id-пространстве
  (таргет предсказывает `▁-`, драфтер `-` — текст один, id разные) → AR≈0.
  Старый пайплайн точно работал в plain-режиме.
- «Родной» шаблон драфтера (`draftchat`) в кросс-комбинации: r≤0.63, хуже.
- Ренормализация на target top-10 (как в текущем `src/eval/`): r заметно ниже
  на offset 0 и схлопывает разброс.

## Файлы

| Файл | Назначение |
|---|---|
| `results/reference_ar_first1_token.txt` / `.json` | эталонные 66 чисел (baseline + sft) |
| `results/ar_per_cluster_first1_token.jpg` | исходный график |
| `src/repro/cache_first_token.py` | шаг 1 — кэш логитов первого токена |
| `src/repro/sweep_first_token.py` | шаг 2 — свип sampling-параметров по кэшу |
| `src/repro/plot_first_token.py` | шаг 4 — график в верстке оригинала |
| `results/REPORT_acceptance_rate_discrepancy.md` | предыдущий анализ расхождения (v1) |
